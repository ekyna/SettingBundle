<ul class="nav nav-tabs">
    <li class="active"><a href="#setting-helper-general" data-toggle="tab">{{ 'ekyna_core.field.general'|trans }}</a></li>
</ul>
<div class="panel-body form-body">
    {{ bootstrap_set_style('horizontal') }}
    {{ form_errors(form) }}
    <div class="tab-content">
	    <div class="tab-pane active" id="setting-helper-general">
	        {{ form_row(form.name) }}
            <div class="row">
                <div class="col-md-6">{{ form_row(form.reference, {'label_col': 4, 'widget_col': 8}) }}</div>
                <div class="col-md-6">{{ form_row(form.enabled, {'label_col': 4, 'widget_col': 8}) }}</div>
            </div>
	        {{ form_row(form.content) }}
	    </div>
        {{ form_rest(form) }}
    </div>
</div>